// `export {}` makes this a module file so the `declare module` blocks below
// act as AUGMENTATIONS (additions to existing types) rather than ambient
// replacements that would shadow the real react-native declarations.
export {};
